# Versions below are the ones the reported results were produced with
# (Python 3.11.15, CUDA 12.1, single NVIDIA RTX 3090 24 GB).
torch==2.5.1
timm==1.0.26
pytorch-lightning==2.6.1
numpy==2.4.3
h5py==3.16.0
scikit-learn==1.8.0
pandas==3.0.2
scipy==1.17.1
matplotlib==3.10.9
tensorboard

# Only needed by scripts/prepare_h5_data.py, which rasterises the source
# products into the HDF5 layout. Install via conda: `conda install -c conda-forge gdal`
# gdal
